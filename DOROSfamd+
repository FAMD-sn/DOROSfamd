<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>DOROSfamd+ - Умный браузер</title>
    <style>
        :root {
            --primary-dark: #0a1e14;
            --primary-green: #1a7a5a;
            --accent-gold: #d4af37;
            --light-text: #f0f0f0;
            --dark-text: #121212;
            --bg-color: #0a0a0a;
            --card-bg: #1a1a1a;
            --border-color: #333;
            --tab-active: #1a3a2d;
            --url-bar: #121212;
            --safe-area-inset-bottom: env(safe-area-inset-bottom, 0px);
            --error-color: #ff6b6b;
            --success-color: #4caf50;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            background-color: var(--bg-color);
            color: var(--light-text);
            height: 100vh;
            display: flex;
            flex-direction: column;
            overflow: hidden;
            padding-bottom: var(--safe-area-inset-bottom);
        }

        /* Навигационная панель */
        .navbar {
            display: flex;
            background-color: var(--primary-dark);
            padding: 0.5rem 0.8rem;
            align-items: center;
            border-bottom: 1px solid var(--accent-gold);
            position: relative;
            z-index: 10;
        }

        .nav-buttons {
            display: flex;
            gap: 0.5rem;
            margin-right: 0.5rem;
        }

        .nav-button {
            background: none;
            border: none;
            color: var(--light-text);
            width: 36px;
            height: 36px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: background-color 0.2s;
            font-size: 1rem;
        }

        .nav-button:hover {
            background-color: rgba(255, 255, 255, 0.1);
        }

        .nav-button:disabled {
            opacity: 0.5;
            cursor: default;
        }

        .logo {
            font-size: 1.1rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--accent-gold), #f5d062);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-right: 0.8rem;
            display: flex;
            align-items: center;
            white-space: nowrap;
        }

        .logo-icon {
            margin-right: 0.4rem;
            font-size: 1.1rem;
        }

        /* Адресная строка */
        .url-bar {
            flex: 1;
            display: flex;
            background-color: var(--url-bar);
            border-radius: 18px;
            padding: 0.3rem 0.8rem;
            margin: 0 0.5rem;
            align-items: center;
            min-width: 0;
            height: 36px;
        }

        .url-input {
            flex: 1;
            background: none;
            border: none;
            color: var(--light-text);
            font-size: 0.92rem;
            padding: 0.3rem;
            outline: none;
            min-width: 0;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .security-indicator {
            margin-right: 0.4rem;
            color: var(--accent-gold);
            font-size: 0.8rem;
            flex-shrink: 0;
        }

        /* Вкладки */
        .tabs-container {
            display: flex;
            background-color: var(--primary-dark);
            padding: 0 0.8rem;
            overflow-x: auto;
            scrollbar-width: none;
            -webkit-overflow-scrolling: touch;
        }

        .tabs-container::-webkit-scrollbar {
            display: none;
        }

        .tab {
            padding: 0.6rem 1rem;
            background-color: var(--primary-dark);
            color: var(--light-text);
            border: none;
            border-top-left-radius: 8px;
            border-top-right-radius: 8px;
            margin-right: 0.2rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            max-width: 180px;
            min-width: 80px;
            position: relative;
            flex-shrink: 0;
            height: 44px;
        }

        .tab.active {
            background-color: var(--tab-active);
            font-weight: 500;
        }

        .tab-title {
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            flex: 1;
            text-align: left;
            font-size: 0.85rem;
        }

        .tab-close {
            margin-left: 0.5rem;
            opacity: 0;
            transition: opacity 0.2s;
            font-size: 0.8rem;
            display: flex;
            align-items: center;
        }

        .tab:hover .tab-close, .tab.active .tab-close {
            opacity: 0.7;
        }

        .add-tab {
            width: 36px;
            min-width: 36px;
            height: 36px;
            display: flex;
            align-items: center;
            justify-content: center;
            background-color: var(--primary-dark);
            color: var(--light-text);
            border: none;
            border-radius: 50%;
            margin-left: 0.5rem;
            cursor: pointer;
            flex-shrink: 0;
            align-self: center;
        }

        .add-tab:hover {
            background-color: var(--primary-green);
        }

        /* Контейнер веб-страницы */
        .browser-content {
            flex: 1;
            display: flex;
            position: relative;
            overflow: hidden;
        }

        .webview-container {
            flex: 1;
            display: none;
            background-color: white;
            position: relative;
        }

        .webview-container.active {
            display: block;
        }

        .webview {
            width: 100%;
            height: 100%;
            border: none;
        }

        /* Стартовая страница */
        .start-page {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            padding: 1.5rem 1rem 2rem;
            background-color: var(--bg-color);
            color: var(--light-text);
            overflow-y: auto;
            -webkit-overflow-scrolling: touch;
        }

        .search-section {
            width: 100%;
            max-width: 700px;
            margin-bottom: 2rem;
            text-align: center;
        }

        .search-container {
            position: relative;
            margin: 0 auto 1.5rem;
            width: 100%;
        }

        .search-input {
            width: 100%;
            padding: 0.9rem 1.2rem;
            font-size: 1rem;
            border-radius: 24px;
            border: 2px solid var(--primary-green);
            background-color: var(--card-bg);
            color: var(--light-text);
            outline: none;
            transition: all 0.3s ease;
            -webkit-appearance: none;
        }

        .search-input:focus {
            border-color: var(--accent-gold);
            box-shadow: 0 0 0 3px rgba(212, 175, 55, 0.3);
        }

        .search-icon {
            position: absolute;
            right: 1.2rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--accent-gold);
            cursor: pointer;
            background: none;
            border: none;
            padding: 0.5rem;
            font-size: 1rem;
        }

        .quick-links {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(90px, 1fr));
            gap: 1.2rem;
            width: 100%;
            max-width: 800px;
        }

        .quick-link {
            display: flex;
            flex-direction: column;
            align-items: center;
            text-decoration: none;
            color: var(--light-text);
            padding: 0.8rem;
            border-radius: 8px;
            transition: all 0.3s ease;
        }

        .quick-link:hover {
            background-color: var(--card-bg);
            transform: translateY(-3px);
        }

        .quick-link-icon {
            width: 44px;
            height: 44px;
            background-color: var(--primary-dark);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 0.6rem;
            font-size: 1.3rem;
        }

        .quick-link-text {
            font-size: 0.85rem;
            text-align: center;
            line-height: 1.2;
        }

        /* Панель инструментов */
        .toolbar {
            display: flex;
            background-color: var(--primary-dark);
            padding: 0.5rem 0.8rem;
            justify-content: space-between;
            border-top: 1px solid var(--border-color);
            position: relative;
            z-index: 10;
            padding-bottom: calc(0.5rem + var(--safe-area-inset-bottom));
        }

        .toolbar-group {
            display: flex;
            gap: 0.4rem;
        }

        .tool-button {
            background: none;
            border: none;
            color: var(--light-text);
            padding: 0.5rem;
            border-radius: 4px;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.3rem;
            font-size: 0.85rem;
            white-space: nowrap;
        }

        .tool-button:hover {
            background-color: rgba(255, 255, 255, 0.1);
        }

        .tool-button i {
            font-size: 1rem;
        }

        /* Меню */
        .menu {
            position: relative;
        }

        .menu-content {
            display: none;
            position: absolute;
            bottom: calc(100% + 5px);
            right: 0;
            background-color: var(--card-bg);
            min-width: 200px;
            border-radius: 8px;
            box-shadow: 0 8px 16px rgba(0, 0, 0, 0.3);
            z-index: 100;
            padding: 0.5rem 0;
            border: 1px solid var(--border-color);
        }

        .menu:hover .menu-content {
            display: block;
        }

        .menu-item {
            color: var(--light-text);
            padding: 0.7rem 1rem;
            text-decoration: none;
            display: block;
            text-align: left;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 0.7rem;
        }

        .menu-item:hover {
            background-color: var(--primary-green);
        }

        /* Адаптивность для мобильных */
        @media (max-width: 768px) {
            .navbar {
                padding: 0.4rem 0.6rem;
            }
            
            .nav-button {
                width: 34px;
                height: 34px;
                font-size: 0.95rem;
            }
            
            .logo {
                font-size: 1rem;
                margin-right: 0.6rem;
            }
            
            .url-bar {
                margin: 0 0.3rem;
                height: 34px;
            }
            
            .url-input {
                font-size: 0.88rem;
            }
            
            .tab {
                min-width: 70px;
                padding: 0.5rem 0.8rem;
                height: 40px;
            }
            
            .tab-title {
                font-size: 0.8rem;
            }
            
            .quick-links {
                grid-template-columns: repeat(auto-fill, minmax(80px, 1fr));
                gap: 1rem;
            }
            
            .quick-link-icon {
                width: 40px;
                height: 40px;
                font-size: 1.1rem;
            }
            
            .tool-button span {
                display: none;
            }
            
            .tool-button i {
                font-size: 1.1rem;
            }
            
            .search-input {
                padding: 0.8rem 1.1rem;
                font-size: 0.95rem;
            }
            
            .search-icon {
                right: 1rem;
            }
        }

        @media (max-width: 480px) {
            .nav-buttons:first-child {
                display: none;
            }
            
            .logo {
                margin-right: 0.4rem;
            }
            
            .tab {
                min-width: 60px;
                max-width: 120px;
                padding: 0.4rem 0.6rem;
            }
            
            .tab-title {
                font-size: 0.75rem;
            }
            
            .tab-close {
                margin-left: 0.3rem;
                font-size: 0.7rem;
            }
            
            .quick-links {
                grid-template-columns: repeat(auto-fill, minmax(70px, 1fr));
                gap: 0.8rem;
            }
            
            .quick-link {
                padding: 0.6rem;
            }
            
            .quick-link-text {
                font-size: 0.8rem;
            }
            
            .toolbar {
                justify-content: space-around;
                padding: 0.4rem 0.6rem;
                padding-bottom: calc(0.4rem + var(--safe-area-inset-bottom));
            }
            
            .toolbar-group {
                gap: 0;
            }
            
            .tool-button {
                padding: 0.6rem;
            }
        }

        /* Анимации */
        @keyframes fadeIn {
            from { opacity: 0; }
            to { opacity: 1; }
        }

        .fade-in {
            animation: fadeIn 0.3s ease-out;
        }

        /* Загрузка */
        .loader {
            display: none;
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            text-align: center;
            z-index: 10;
            background-color: rgba(0, 0, 0, 0.7);
            padding: 1.5rem 2rem;
            border-radius: 8px;
        }

        .loader-spinner {
            border: 4px solid rgba(30, 107, 78, 0.3);
            border-radius: 50%;
            border-top: 4px solid var(--accent-gold);
            width: 40px;
            height: 40px;
            animation: spin 1s linear infinite;
            margin: 0 auto 1rem;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        /* Блокировщик рекламы */
        .ad-blocked {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: rgba(0, 0, 0, 0.7);
            color: white;
            padding: 0.3rem 0.6rem;
            border-radius: 4px;
            font-size: 0.8rem;
            z-index: 100;
        }

        /* Поисковые подсказки */
        .search-suggestions {
            position: absolute;
            top: 100%;
            left: 0;
            right: 0;
            background-color: var(--card-bg);
            border-radius: 0 0 12px 12px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            z-index: 50;
            margin-top: -8px;
            border: 1px solid var(--border-color);
            border-top: none;
            max-height: 300px;
            overflow-y: auto;
            display: none;
        }

        .suggestion-item {
            padding: 0.8rem 1.2rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            border-bottom: 1px solid var(--border-color);
        }

        .suggestion-item:last-child {
            border-bottom: none;
        }

        .suggestion-item:hover {
            background-color: var(--primary-dark);
        }

        .suggestion-icon {
            margin-right: 0.8rem;
            color: var(--accent-gold);
        }

        /* Мобильное меню */
        .mobile-menu {
            display: none;
            position: fixed;
            bottom: calc(60px + var(--safe-area-inset-bottom));
            right: 15px;
            background-color: var(--card-bg);
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
            z-index: 1000;
            padding: 0.8rem 0;
            border: 1px solid var(--border-color);
            min-width: 200px;
        }

        .mobile-menu-item {
            padding: 0.8rem 1.2rem;
            display: flex;
            align-items: center;
            color: var(--light-text);
            text-decoration: none;
        }

        .mobile-menu-item i {
            margin-right: 0.8rem;
            width: 20px;
            text-align: center;
        }

        .mobile-menu-item:hover {
            background-color: var(--primary-dark);
        }

        /* Кнопка мобильного меню */
        .mobile-menu-button {
            display: none;
            position: fixed;
            bottom: calc(15px + var(--safe-area-inset-bottom));
            right: 15px;
            width: 50px;
            height: 50px;
            background-color: var(--primary-green);
            border-radius: 50%;
            border: none;
            color: white;
            font-size: 1.2rem;
            z-index: 100;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
        }

        @media (max-width: 768px) {
            .mobile-menu-button {
                display: flex;
                align-items: center;
                justify-content: center;
            }
            
            .toolbar {
                padding-bottom: calc(0.5rem + var(--safe-area-inset-bottom) + 60px);
            }
        }

        /* Улучшенный скроллбар */
        ::-webkit-scrollbar {
            width: 6px;
            height: 6px;
        }

        ::-webkit-scrollbar-track {
            background: var(--card-bg);
        }

        ::-webkit-scrollbar-thumb {
            background: var(--primary-green);
            border-radius: 3px;
        }

        /* Анимация вкладок */
        .tab-slide-enter {
            transform: translateX(20px);
            opacity: 0;
        }

        .tab-slide-enter-active {
            transform: translateX(0);
            opacity: 1;
            transition: all 0.3s ease;
        }

        .tab-slide-exit {
            transform: translateX(0);
            opacity: 1;
        }

        .tab-slide-exit-active {
            transform: translateX(-20px);
            opacity: 0;
            transition: all 0.3s ease;
        }

        /* Оверлей настроек */
        .settings-overlay {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: rgba(0, 0, 0, 0.8);
            z-index: 1000;
            align-items: center;
            justify-content: center;
            padding: 1rem;
        }

        .settings-container {
            background-color: var(--card-bg);
            border-radius: 12px;
            max-width: 500px;
            width: 100%;
            max-height: 80vh;
            overflow-y: auto;
            padding: 1.5rem;
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.3);
        }

        .settings-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 1.5rem;
        }

        .settings-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--accent-gold);
        }

        .close-settings {
            background: none;
            border: none;
            color: var(--light-text);
            font-size: 1.5rem;
            cursor: pointer;
        }

        .settings-section {
            margin-bottom: 1.5rem;
        }

        .settings-section-title {
            font-size: 1rem;
            font-weight: 500;
            margin-bottom: 0.8rem;
            color: var(--accent-gold);
        }

        .setting-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 0.8rem 0;
            border-bottom: 1px solid var(--border-color);
        }

        .setting-label {
            font-size: 0.95rem;
        }

        .switch {
            position: relative;
            display: inline-block;
            width: 50px;
            height: 24px;
        }

        .switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #555;
            transition: .4s;
            border-radius: 24px;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 16px;
            width: 16px;
            left: 4px;
            bottom: 4px;
            background-color: white;
            transition: .4s;
            border-radius: 50%;
        }

        input:checked + .slider {
            background-color: var(--primary-green);
        }

        input:checked + .slider:before {
            transform: translateX(26px);
        }

        /* Режим чтения */
        .reader-mode {
            background-color: #f8f4e8 !important;
            color: #333 !important;
            font-family: Georgia, serif !important;
            line-height: 1.6 !important;
            padding: 2rem !important;
            max-width: 800px !important;
            margin: 0 auto !important;
        }

        .reader-mode img,
        .reader-mode video,
        .reader-mode iframe,
        .reader-mode .ad,
        .reader-mode .ads {
            display: none !important;
        }

        .reader-mode h1,
        .reader-mode h2,
        .reader-mode h3 {
            color: #222 !important;
            margin-bottom: 1rem !important;
        }

        .reader-mode p {
            margin-bottom: 1.2rem !important;
        }
    </style>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body>
    <!-- Навигационная панель -->
    <div class="navbar">
        <div class="nav-buttons">
            <button class="nav-button" id="back-button" disabled>
                <i class="fas fa-arrow-left"></i>
            </button>
            <button class="nav-button" id="forward-button" disabled>
                <i class="fas fa-arrow-right"></i>
            </button>
            <button class="nav-button" id="refresh-button">
                <i class="fas fa-sync-alt"></i>
            </button>
        </div>

        <div class="logo">
            <i class="fas fa-globe logo-icon"></i>
            <span>DOROSfamd+</span>
        </div>

        <div class="url-bar">
            <span class="security-indicator" id="security-indicator">
                <i class="fas fa-lock"></i>
            </span>
            <input type="text" class="url-input" id="url-input" placeholder="Поиск или введите адрес" autocomplete="off">
            <button class="nav-button" id="go-button">
                <i class="fas fa-search"></i>
            </button>
        </div>

        <div class="nav-buttons">
            <button class="nav-button" id="bookmark-button">
                <i class="far fa-star"></i>
            </button>
            <button class="nav-button" id="menu-button">
                <i class="fas fa-ellipsis-v"></i>
            </button>
        </div>
    </div>

    <!-- Вкладки -->
    <div class="tabs-container" id="tabs-container">
        <!-- Вкладки будут добавляться здесь -->
        <button class="add-tab" id="add-tab">
            <i class="fas fa-plus"></i>
        </button>
    </div>

    <!-- Контейнер браузера -->
    <div class="browser-content" id="browser-content">
        <!-- Веб-страницы будут загружаться здесь -->
    </div>

    <!-- Панель инструментов -->
    <div class="toolbar">
        <div class="toolbar-group">
            <button class="tool-button" id="home-button">
                <i class="fas fa-home"></i>
                <span>Главная</span>
            </button>
            <button class="tool-button" id="history-button">
                <i class="fas fa-history"></i>
                <span>История</span>
            </button>
            <button class="tool-button" id="downloads-button">
                <i class="fas fa-download"></i>
                <span>Загрузки</span>
            </button>
        </div>
        <div class="toolbar-group">
            <button class="tool-button" id="ad-block-button">
                <i class="fas fa-shield-alt"></i>
                <span>Блокировщик</span>
            </button>
            <button class="tool-button" id="reader-mode-button">
                <i class="fas fa-book-open"></i>
                <span>Чтение</span>
            </button>
        </div>
        <div class="toolbar-group">
            <div class="menu">
                <button class="tool-button" id="settings-button">
                    <i class="fas fa-cog"></i>
                    <span>Настройки</span>
                </button>
                <div class="menu-content">
                    <a href="#" class="menu-item" id="theme-dark"><i class="fas fa-moon"></i> Тёмная тема</a>
                    <a href="#" class="menu-item" id="theme-light"><i class="fas fa-sun"></i> Светлая тема</a>
                    <a href="#" class="menu-item" id="clear-data"><i class="fas fa-database"></i> Очистить данные</a>
                    <a href="#" class="menu-item" id="about-browser"><i class="fas fa-info-circle"></i> О браузере</a>
                </div>
            </div>
        </div>
    </div>

    <!-- Кнопка мобильного меню -->
    <button class="mobile-menu-button" id="mobile-menu-button">
        <i class="fas fa-bars"></i>
    </button>

    <!-- Мобильное меню -->
    <div class="mobile-menu" id="mobile-menu">
        <a href="#" class="mobile-menu-item" id="mobile-home-button">
            <i class="fas fa-home"></i>
            <span>Главная</span>
        </a>
        <a href="#" class="mobile-menu-item" id="mobile-history-button">
            <i class="fas fa-history"></i>
            <span>История</span>
        </a>
        <a href="#" class="mobile-menu-item" id="mobile-downloads-button">
            <i class="fas fa-download"></i>
            <span>Загрузки</span>
        </a>
        <a href="#" class="mobile-menu-item" id="mobile-ad-block-button">
            <i class="fas fa-shield-alt"></i>
            <span>Блокировщик</span>
        </a>
        <a href="#" class="mobile-menu-item" id="mobile-reader-mode-button">
            <i class="fas fa-book-open"></i>
            <span>Режим чтения</span>
        </a>
        <a href="#" class="mobile-menu-item" id="mobile-settings-button">
            <i class="fas fa-cog"></i>
            <span>Настройки</span>
        </a>
    </div>

    <!-- Загрузчик -->
    <div class="loader" id="loader">
        <div class="loader-spinner"></div>
        <p>Загрузка...</p>
    </div>

    <!-- Поисковые подсказки -->
    <div class="search-suggestions" id="search-suggestions">
        <!-- Подсказки будут добавляться здесь -->
    </div>

    <!-- Оверлей настроек -->
    <div class="settings-overlay" id="settings-overlay">
        <div class="settings-container">
            <div class="settings-header">
                <h2 class="settings-title">Настройки браузера</h2>
                <button class="close-settings" id="close-settings">&times;</button>
            </div>
            
            <div class="settings-section">
                <h3 class="settings-section-title">Внешний вид</h3>
                <div class="setting-item">
                    <span class="setting-label">Тёмная тема</span>
                    <label class="switch">
                        <input type="checkbox" id="dark-mode-toggle" checked>
                        <span class="slider"></span>
                    </label>
                </div>
            </div>
            
            <div class="settings-section">
                <h3 class="settings-section-title">Поиск</h3>
                <div class="setting-item">
                    <span class="setting-label">Поисковая система</span>
                    <select id="search-engine-select">
                        <option value="google">Google</option>
                        <option value="yandex">Яндекс</option>
                        <option value="duckduckgo">DuckDuckGo</option>
                        <option value="bing">Bing</option>
                    </select>
                </div>
            </div>
            
            <div class="settings-section">
                <h3 class="settings-section-title">Конфиденциальность</h3>
                <div class="setting-item">
                    <span class="setting-label">Блокировщик рекламы</span>
                    <label class="switch">
                        <input type="checkbox" id="ad-block-toggle" checked>
                        <span class="slider"></span>
                    </label>
                </div>
            </div>
            
            <div class="settings-section">
                <h3 class="settings-section-title">О браузере</h3>
                <p style="margin-bottom: 1rem;">DOROSfamd+ Browser v1.0</p>
                <p>Быстрый и безопасный браузер с блокировщиком рекламы.</p>
            </div>
        </div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // Состояние браузера
            const state = {
                tabs: [],
                activeTab: null,
                history: [],
                bookmarks: [],
                settings: {
                    adBlockEnabled: true,
                    darkMode: true,
                    defaultSearchEngine: 'https://www.google.com/search?q=',
                    homePage: 'startpage',
                    searchEngines: {
                        'google': 'https://www.google.com/search?q=',
                        'yandex': 'https://yandex.ru/search/?text=',
                        'duckduckgo': 'https://duckduckgo.com/?q=',
                        'bing': 'https://www.bing.com/search?q='
                    },
                    quickLinks: [
                        { name: "Google", url: "https://www.google.com", icon: "fab fa-google" },
                        { name: "YouTube", url: "https://www.youtube.com", icon: "fab fa-youtube" },
                        { name: "Wikipedia", url: "https://www.wikipedia.org", icon: "fab fa-wikipedia-w" },
                        { name: "GitHub", url: "https://github.com", icon: "fab fa-github" },
                        { name: "Twitter", url: "https://twitter.com", icon: "fab fa-twitter" },
                        { name: "Reddit", url: "https://www.reddit.com", icon: "fab fa-reddit" },
                        { name: "Яндекс", url: "https://yandex.ru", icon: "fas fa-search" },
                        { name: "BBC News", url: "https://www.bbc.com", icon: "fas fa-newspaper" }
                    ]
                }
            };

            // Элементы DOM
            const tabsContainer = document.getElementById('tabs-container');
            const browserContent = document.getElementById('browser-content');
            const addTabButton = document.getElementById('add-tab');
            const urlInput = document.getElementById('url-input');
            const goButton = document.getElementById('go-button');
            const backButton = document.getElementById('back-button');
            const forwardButton = document.getElementById('forward-button');
            const refreshButton = document.getElementById('refresh-button');
            const homeButton = document.getElementById('home-button');
            const adBlockButton = document.getElementById('ad-block-button');
            const readerModeButton = document.getElementById('reader-mode-button');
            const securityIndicator = document.getElementById('security-indicator');
            const loader = document.getElementById('loader');
            const searchSuggestions = document.getElementById('search-suggestions');
            const mobileMenuButton = document.getElementById('mobile-menu-button');
            const mobileMenu = document.getElementById('mobile-menu');
            const settingsOverlay = document.getElementById('settings-overlay');
            const closeSettings = document.getElementById('close-settings');
            const darkModeToggle = document.getElementById('dark-mode-toggle');
            const adBlockToggle = document.getElementById('ad-block-toggle');
            const searchEngineSelect = document.getElementById('search-engine-select');
            const themeDarkButton = document.getElementById('theme-dark');
            const themeLightButton = document.getElementById('theme-light');
            const clearDataButton = document.getElementById('clear-data');
            const aboutBrowserButton = document.getElementById('about-browser');

            // Популярные поисковые запросы (для демонстрации)
            const popularSearches = {
                'погода': [
                    { query: 'погода в Худжанде', url: 'https://www.google.com/search?q=погода+в+Худжанде' },
                    { query: 'погода в Душанбе', url: 'https://www.google.com/search?q=погода+в+Душанбе' },
                    { query: 'погода сейчас', url: 'https://www.google.com/search?q=погода+сейчас' }
                ],
                'новости': [
                    { query: 'новости Таджикистана', url: 'https://www.google.com/search?q=новости+Таджикистана' },
                    { query: 'последние новости', url: 'https://www.google.com/search?q=последние+новости' }
                ],
                'курс': [
                    { query: 'курс доллара', url: 'https://www.google.com/search?q=курс+доллара' },
                    { query: 'курс сомони', url: 'https://www.google.com/search?q=курс+сомони' }
                ]
            };

            // Инициализация браузера
            function initBrowser() {
                // Загружаем настройки из localStorage
                loadSettings();
                
                // Создаём стартовую вкладку
                createNewTab();
                
                // Устанавливаем обработчики событий
                setupEventListeners();
            }

            // Загрузка настроек
            function loadSettings() {
                const savedSettings = localStorage.getItem('browserSettings');
                if (savedSettings) {
                    state.settings = JSON.parse(savedSettings);
                }
                
                // Применяем настройки
                applySettings();
            }

            // Сохранение настроек
            function saveSettings() {
                localStorage.setItem('browserSettings', JSON.stringify(state.settings));
            }

            // Применение настроек
            function applySettings() {
                // Тёмная тема
                if (state.settings.darkMode) {
                    document.documentElement.style.setProperty('--bg-color', '#0a0a0a');
                    document.documentElement.style.setProperty('--light-text', '#f0f0f0');
                } else {
                    document.documentElement.style.setProperty('--bg-color', '#f0f0f0');
                    document.documentElement.style.setProperty('--light-text', '#121212');
                }
                
                // Блокировщик рекламы
                adBlockButton.innerHTML = state.settings.adBlockEnabled ? 
                    '<i class="fas fa-shield-alt"></i><span>Блокировщик</span>' : 
                    '<i class="fas fa-shield-alt" style="opacity: 0.5;"></i><span>Блокировщик</span>';
                
                // Обновляем мобильную кнопку блокировщика
                const mobileAdBlockButton = document.getElementById('mobile-ad-block-button');
                if (mobileAdBlockButton) {
                    mobileAdBlockButton.innerHTML = state.settings.adBlockEnabled ? 
                        '<i class="fas fa-shield-alt"></i><span>Блокировщик (вкл)</span>' : 
                        '<i class="fas fa-shield-alt"></i><span>Блокировщик (выкл)</span>';
                }
            }

            // Создание новой вкладки
            function createNewTab(url = null, isActive = true) {
                const tabId = 'tab-' + Date.now();
                const tabTitle = 'Новая вкладка';
                
                // Создаём элемент вкладки
                const tabElement = document.createElement('button');
                tabElement.className = 'tab' + (isActive ? ' active' : '');
                tabElement.id = tabId + '-tab';
                tabElement.innerHTML = `
                    <span class="tab-title">${tabTitle}</span>
                    <span class="tab-close"><i class="fas fa-times"></i></span>
                `;
                
                // Вставляем перед кнопкой добавления
                tabsContainer.insertBefore(tabElement, addTabButton);
                
                // Создаём контейнер для веб-страницы
                const webviewContainer = document.createElement('div');
                webviewContainer.className = 'webview-container' + (isActive ? ' active' : '');
                webviewContainer.id = tabId + '-content';
                
                // Если это стартовая страница
                if (url === 'startpage') {
                    webviewContainer.innerHTML = `
                        <div class="start-page">
                            <div class="search-section">
                                <div class="search-container">
                                    <input type="text" class="search-input" id="startpage-search" placeholder="Поиск в интернете" autocomplete="off">
                                    <button class="search-icon" id="startpage-search-button"><i class="fas fa-search"></i></button>
                                </div>
                                <div class="search-suggestions" id="startpage-suggestions"></div>
                            </div>
                            <div class="quick-links" id="quick-links">
                                <!-- Быстрые ссылки будут здесь -->
                            </div>
                        </div>
                    `;
                } else {
                    // Создаём iframe для веб-страницы
                    const webview = document.createElement('iframe');
                    webview.className = 'webview';
                    webview.id = tabId + '-webview';
                    webview.allow = 'fullscreen';
                    webview.setAttribute('sandbox', 'allow-same-origin allow-scripts allow-popups allow-forms');
                    
                    if (url) {
                        webview.src = prepareUrl(url);
                    }
                    
                    webviewContainer.appendChild(webview);
                }
                
                browserContent.appendChild(webviewContainer);
                
                // Добавляем вкладку в состояние
                const tab = {
                    id: tabId,
                    title: tabTitle,
                    url: url || '',
                    element: tabElement,
                    container: webviewContainer,
                    history: [],
                    historyIndex: -1,
                    favicon: null
                };
                
                state.tabs.push(tab);
                
                if (isActive) {
                    setActiveTab(tabId);
                }
                
                // Обработчики событий для вкладки
                tabElement.addEventListener('click', () => setActiveTab(tabId));
                
                const closeButton = tabElement.querySelector('.tab-close');
                closeButton.addEventListener('click', (e) => {
                    e.stopPropagation();
                    closeTab(tabId);
                });
                
                return tab;
            }

            // Установка активной вкладки
            function setActiveTab(tabId) {
                // Находим вкладку
                const tab = state.tabs.find(t => t.id === tabId);
                if (!tab) return;
                
                // Обновляем состояние
                state.activeTab = tab;
                
                // Обновляем UI вкладок
                state.tabs.forEach(t => {
                    t.element.classList.remove('active');
                    t.container.classList.remove('active');
                });
                
                tab.element.classList.add('active');
                tab.container.classList.add('active');
                
                // Обновляем адресную строку
                updateUrlBar(tab.url);
                
                // Обновляем кнопки навигации
                updateNavButtons();
                
                // Скрываем подсказки поиска
                hideSearchSuggestions();
            }

            // Закрытие вкладки
            function closeTab(tabId) {
                if (state.tabs.length <= 1) {
                    // Нельзя закрыть последнюю вкладку
                    createNewTab();
                }
                
                const tabIndex = state.tabs.findIndex(t => t.id === tabId);
                if (tabIndex === -1) return;
                
                const tab = state.tabs[tabIndex];
                
                // Удаляем элементы из DOM
                tab.element.remove();
                tab.container.remove();
                
                // Удаляем из состояния
                state.tabs.splice(tabIndex, 1);
                
                // Если закрыли активную вкладку, активируем предыдущую
                if (tabId === state.activeTab?.id) {
                    const newActiveTab = state.tabs[Math.max(0, tabIndex - 1)];
                    if (newActiveTab) {
                        setActiveTab(newActiveTab.id);
                    }
                }
            }

            // Обновление адресной строки
            function updateUrlBar(url) {
                urlInput.value = url || '';
                
                // Обновляем индикатор безопасности
                if (url && url.startsWith('https://')) {
                    securityIndicator.innerHTML = '<i class="fas fa-lock"></i>';
                    securityIndicator.style.color = 'var(--accent-gold)';
                } else if (url) {
                    securityIndicator.innerHTML = '<i class="fas fa-unlock"></i>';
                    securityIndicator.style.color = '#ff6b6b';
                } else {
                    securityIndicator.innerHTML = '';
                }
            }

            // Обновление кнопок навигации
            function updateNavButtons() {
                if (!state.activeTab) return;
                
                backButton.disabled = state.activeTab.historyIndex <= 0;
                forwardButton.disabled = state.activeTab.historyIndex >= state.activeTab.history.length - 1;
            }

            // Подготовка URL
            function prepareUrl(input) {
                if (!input) return '';
                
                // Если это URL (начинается с http:// или https://)
                if (/^https?:\/\//i.test(input)) {
                    return input;
                }
                
                // Если это доменное имя (содержит точку, но не пробелы)
                if (input.includes('.') && !input.includes(' ')) {
                    return 'https://' + input;
                }
                
                // В противном случае считаем это поисковым запросом
                return state.settings.defaultSearchEngine + encodeURIComponent(input);
            }

            // Навигация по URL
            function navigateTo(url) {
                if (!state.activeTab) return;
                
                const preparedUrl = prepareUrl(url);
                if (!preparedUrl) return;
                
                // Добавляем в историю
                if (state.activeTab.url !== preparedUrl) {
                    // Удаляем всё после текущего индекса
                    state.activeTab.history = state.activeTab.history.slice(0, state.activeTab.historyIndex + 1);
                    
                    // Добавляем новый URL
                    state.activeTab.history.push(preparedUrl);
                    state.activeTab.historyIndex = state.activeTab.history.length - 1;
                }
                
                state.activeTab.url = preparedUrl;
                updateUrlBar(preparedUrl);
                
                // Показываем загрузчик
                showLoader();
                
                // Если это стартовая страница
                if (preparedUrl === 'startpage') {
                    state.activeTab.container.innerHTML = `
                        <div class="start-page">
                            <div class="search-section">
                                <div class="search-container">
                                    <input type="text" class="search-input" id="startpage-search" placeholder="Поиск в интернете" autocomplete="off">
                                    <button class="search-icon" id="startpage-search-button"><i class="fas fa-search"></i></button>
                                </div>
                                <div class="search-suggestions" id="startpage-suggestions"></div>
                            </div>
                            <div class="quick-links" id="quick-links">
                                <!-- Быстрые ссылки будут здесь -->
                            </div>
                        </div>
                    `;
                    
                    // Инициализируем стартовую страницу
                    initStartPage(state.activeTab.container);
                    hideLoader();
                    return;
                }
                
                // Создаём iframe, если его нет
                let webview = state.activeTab.container.querySelector('.webview');
                if (!webview) {
                    webview = document.createElement('iframe');
                    webview.className = 'webview';
                    webview.id = state.activeTab.id + '-webview';
                    webview.allow = 'fullscreen';
                    webview.setAttribute('sandbox', 'allow-same-origin allow-scripts allow-popups allow-forms');
                    state.activeTab.container.innerHTML = '';
                    state.activeTab.container.appendChild(webview);
                }
                
                // Загружаем URL
                webview.src = preparedUrl;
                
                // Обновляем заголовок после загрузки
                webview.addEventListener('load', () => {
                    try {
                        state.activeTab.title = webview.contentDocument.title || webview.src;
                        state.activeTab.element.querySelector('.tab-title').textContent = state.activeTab.title;
                        
                        // Пытаемся получить favicon
                        const favicon = webview.contentDocument.querySelector('link[rel="icon"]') || 
                                        webview.contentDocument.querySelector('link[rel="shortcut icon"]');
                        if (favicon) {
                            state.activeTab.favicon = favicon.href;
                        }
                    } catch (e) {
                        // Ошибка CORS, игнорируем
                    }
                    
                    hideLoader();
                    updateNavButtons();
                    
                    // Применяем блокировщик рекламы
                    if (state.settings.adBlockEnabled) {
                        applyAdBlock(webview);
                    }
                });
                
                // Обработка ошибок
                webview.addEventListener('error', () => {
                    hideLoader();
                    state.activeTab.container.innerHTML = `
                        <div style="padding: 2rem; text-align: center; color: var(--light-text);">
                            <h2>Не удалось загрузить страницу</h2>
                            <p>Проверьте подключение к интернету и правильность URL</p>
                            <button id="retry-button" style="margin-top: 1rem; padding: 0.5rem 1rem; background-color: var(--primary-green); border: none; border-radius: 4px; color: white; cursor: pointer;">
                                Попробовать снова
                            </button>
                        </div>
                    `;
                    
                    document.getElementById('retry-button').addEventListener('click', () => {
                        navigateTo(preparedUrl);
                    });
                });
            }

            // Блокировка рекламы
            function applyAdBlock(webview) {
                try {
                    // Это демонстрационная реализация - в реальном браузере нужен более сложный подход
                    const scripts = webview.contentDocument.querySelectorAll('script');
                    scripts.forEach(script => {
                        if (script.src.includes('ads') || script.src.includes('adservice')) {
                            script.remove();
                        }
                    });
                    
                    const iframes = webview.contentDocument.querySelectorAll('iframe');
                    iframes.forEach(iframe => {
                        if (iframe.src.includes('ads') || iframe.src.includes('doubleclick')) {
                            iframe.remove();
                        }
                    });
                    
                    // Показываем уведомление о блокировке
                    showAdBlockNotification(webview);
                } catch (e) {
                    // Ошибка CORS, не можем получить доступ к содержимому iframe
                }
            }

            // Уведомление о блокировке рекламы
            function showAdBlockNotification(webview) {
                const adBlocked = document.createElement('div');
                adBlocked.className = 'ad-blocked';
                adBlocked.innerHTML = '<i class="fas fa-shield-alt"></i> Реклама заблокирована';
                
                // Вставляем в iframe
                try {
                    webview.contentDocument.body.appendChild(adBlocked);
                    
                    // Автоматически скрываем через 3 секунды
                    setTimeout(() => {
                        adBlocked.style.opacity = '0';
                        setTimeout(() => adBlocked.remove(), 300);
                    }, 3000);
                } catch (e) {
                    // Ошибка CORS
                }
            }

            // Показать загрузчик
            function showLoader() {
                loader.style.display = 'block';
            }

            // Скрыть загрузчик
            function hideLoader() {
                loader.style.display = 'none';
            }

            // Инициализация стартовой страницы
            function initStartPage(container) {
                const quickLinksContainer = container.querySelector('#quick-links');
                quickLinksContainer.innerHTML = '';
                
                // Добавляем быстрые ссылки из настроек
                state.settings.quickLinks.forEach(link => {
                    const linkElement = document.createElement('a');
                    linkElement.className = 'quick-link';
                    linkElement.href = '#';
                    linkElement.innerHTML = `
                        <div class="quick-link-icon">
                            <i class="${link.icon}"></i>
                        </div>
                        <div class="quick-link-text">${link.name}</div>
                    `;
                    
                    linkElement.addEventListener('click', (e) => {
                        e.preventDefault();
                        navigateTo(link.url);
                    });
                    
                    quickLinksContainer.appendChild(linkElement);
                });
                
                // Обработка поиска на стартовой странице
                const searchInput = container.querySelector('#startpage-search');
                const searchButton = container.querySelector('#startpage-search-button');
                const suggestionsContainer = container.querySelector('#startpage-suggestions');
                
                const handleSearch = () => {
                    const query = searchInput.value.trim();
                    if (query) {
                        navigateTo(query);
                    }
                };
                
                searchInput.addEventListener('input', (e) => {
                    showSearchSuggestions(searchInput.value, suggestionsContainer);
                });
                
                searchInput.addEventListener('focus', () => {
                    showSearchSuggestions(searchInput.value, suggestionsContainer);
                });
                
                searchInput.addEventListener('blur', () => {
                    setTimeout(() => {
                        suggestionsContainer.style.display = 'none';
                    }, 200);
                });
                
                searchInput.addEventListener('keypress', (e) => {
                    if (e.key === 'Enter') {
                        handleSearch();
                    }
                });
                
                searchButton.addEventListener('click', handleSearch);
            }

            // Показать поисковые подсказки
            function showSearchSuggestions(query, container) {
                if (!query) {
                    container.innerHTML = '';
                    container.style.display = 'none';
                    return;
                }
                
                container.innerHTML = '';
                
                // Проверяем, есть ли популярные запросы для этого слова
                const firstWord = query.split(' ')[0].toLowerCase();
                if (popularSearches[firstWord]) {
                    popularSearches[firstWord].forEach(item => {
                        const suggestion = document.createElement('div');
                        suggestion.className = 'suggestion-item';
                        suggestion.innerHTML = `
                            <i class="fas fa-search suggestion-icon"></i>
                            <span>${item.query}</span>
                        `;
                        
                        suggestion.addEventListener('click', () => {
                            navigateTo(item.url);
                        });
                        
                        container.appendChild(suggestion);
                    });
                } else {
                    // Общие подсказки
                    const commonSuggestions = [
                        { query: `${query} погода`, url: `https://www.google.com/search?q=${encodeURIComponent(query)}+погода` },
                        { query: `${query} новости`, url: `https://www.google.com/search?q=${encodeURIComponent(query)}+новости` },
                        { query: `${query} карта`, url: `https://www.google.com/maps/search/${encodeURIComponent(query)}` },
                        { query: `${query} видео`, url: `https://www.youtube.com/results?search_query=${encodeURIComponent(query)}` }
                    ];
                    
                    commonSuggestions.forEach(item => {
                        const suggestion = document.createElement('div');
                        suggestion.className = 'suggestion-item';
                        suggestion.innerHTML = `
                            <i class="fas fa-search suggestion-icon"></i>
                            <span>${item.query}</span>
                        `;
                        
                        suggestion.addEventListener('click', () => {
                            navigateTo(item.url);
                        });
                        
                        container.appendChild(suggestion);
                    });
                }
                
                if (container.children.length > 0) {
                    container.style.display = 'block';
                } else {
                    container.style.display = 'none';
                }
            }

            // Скрыть поисковые подсказки
            function hideSearchSuggestions() {
                searchSuggestions.style.display = 'none';
            }

            // Назад в истории
            function goBack() {
                if (!state.activeTab || state.activeTab.historyIndex <= 0) return;
                
                state.activeTab.historyIndex--;
                const url = state.activeTab.history[state.activeTab.historyIndex];
                state.activeTab.url = url;
                updateUrlBar(url);
                
                const webview = state.activeTab.container.querySelector('.webview');
                if (webview) {
                    webview.src = url;
                }
                
                updateNavButtons();
            }

            // Вперёд в истории
            function goForward() {
                if (!state.activeTab || state.activeTab.historyIndex >= state.activeTab.history.length - 1) return;
                
                state.activeTab.historyIndex++;
                const url = state.activeTab.history[state.activeTab.historyIndex];
                state.activeTab.url = url;
                updateUrlBar(url);
                
                const webview = state.activeTab.container.querySelector('.webview');
                if (webview) {
                    webview.src = url;
                }
                
                updateNavButtons();
            }

            // Обновление страницы
            function refreshPage() {
                if (!state.activeTab) return;
                
                const webview = state.activeTab.container.querySelector('.webview');
                if (webview) {
                    webview.src = webview.src;
                } else if (state.activeTab.url === 'startpage') {
                    state.activeTab.container.innerHTML = '';
                    navigateTo('startpage');
                }
            }

            // Режим чтения
            function toggleReaderMode() {
                if (!state.activeTab) return;
                
                const webview = state.activeTab.container.querySelector('.webview');
                if (!webview) return;
                
                try {
                    if (webview.contentDocument.body.classList.contains('reader-mode')) {
                        webview.contentDocument.body.classList.remove('reader-mode');
                        alert('Режим чтения выключен');
                    } else {
                        webview.contentDocument.body.classList.add('reader-mode');
                        alert('Режим чтения включен');
                    }
                } catch (e) {
                    alert('Не удалось применить режим чтения к этой странице из-за ограничений безопасности.');
                }
            }

            // Показать настройки
            function showSettings() {
                settingsOverlay.style.display = 'flex';
                darkModeToggle.checked = state.settings.darkMode;
                adBlockToggle.checked = state.settings.adBlockEnabled;
            }

            // Скрыть настройки
            function hideSettings() {
                settingsOverlay.style.display = 'none';
            }

            // Переключить тёмную тему
            function toggleDarkMode() {
                state.settings.darkMode = !state.settings.darkMode;
                saveSettings();
                applySettings();
            }

            // Переключить блокировщик рекламы
            function toggleAdBlock() {
                state.settings.adBlockEnabled = !state.settings.adBlockEnabled;
                saveSettings();
                applySettings();
            }

            // Очистить данные браузера
            function clearBrowserData() {
                if (confirm('Вы уверены, что хотите очистить все данные (историю, кэш, cookies)?')) {
                    localStorage.clear();
                    state.history = [];
                    state.bookmarks = [];
                    alert('Данные браузера очищены');
                }
            }

            // Установка обработчиков событий
            function setupEventListeners() {
                // Кнопка добавления вкладки
                addTabButton.addEventListener('click', () => createNewTab());
                
                // Навигация по URL
                urlInput.addEventListener('input', () => {
                    showUrlSuggestions(urlInput.value);
                });
                
                urlInput.addEventListener('focus', () => {
                    showUrlSuggestions(urlInput.value);
                });
                
                urlInput.addEventListener('blur', () => {
                    setTimeout(() => {
                        hideSearchSuggestions();
                    }, 200);
                });
                
                urlInput.addEventListener('keypress', (e) => {
                    if (e.key === 'Enter') {
                        navigateTo(urlInput.value);
                        hideSearchSuggestions();
                    }
                });
                
                goButton.addEventListener('click', () => {
                    navigateTo(urlInput.value);
                    hideSearchSuggestions();
                });
                
                // Кнопки навигации
                backButton.addEventListener('click', goBack);
                forwardButton.addEventListener('click', goForward);
                refreshButton.addEventListener('click', refreshPage);
                
                // Кнопка домой
                homeButton.addEventListener('click', () => {
                    navigateTo(state.settings.homePage);
                });
                
                // Блокировщик рекламы
                adBlockButton.addEventListener('click', toggleAdBlock);
                
                // Режим чтения
                readerModeButton.addEventListener('click', toggleReaderMode);
                
                // Мобильное меню
                mobileMenuButton.addEventListener('click', (e) => {
                    e.stopPropagation();
                    mobileMenu.style.display = mobileMenu.style.display === 'block' ? 'none' : 'block';
                });
                
                document.addEventListener('click', (e) => {
                    if (!mobileMenu.contains(e.target)) {
                        mobileMenu.style.display = 'none';
                    }
                });
                
                // Обработчики для мобильного меню
                document.getElementById('mobile-home-button').addEventListener('click', (e) => {
                    e.preventDefault();
                    navigateTo(state.settings.homePage);
                    mobileMenu.style.display = 'none';
                });
                
                document.getElementById('mobile-ad-block-button').addEventListener('click', (e) => {
                    e.preventDefault();
                    toggleAdBlock();
                    mobileMenu.style.display = 'none';
                });
                
                document.getElementById('mobile-reader-mode-button').addEventListener('click', (e) => {
                    e.preventDefault();
                    toggleReaderMode();
                    mobileMenu.style.display = 'none';
                });
                
                document.getElementById('mobile-settings-button').addEventListener('click', (e) => {
                    e.preventDefault();
                    showSettings();
                    mobileMenu.style.display = 'none';
                });
                
                // Настройки
                settingsButton.addEventListener('click', showSettings);
                closeSettings.addEventListener('click', hideSettings);
                darkModeToggle.addEventListener('change', toggleDarkMode);
                adBlockToggle.addEventListener('change', toggleAdBlock);
                
                // Быстрое переключение темы
                themeDarkButton.addEventListener('click', (e) => {
                    e.preventDefault();
                    state.settings.darkMode = true;
                    saveSettings();
                    applySettings();
                });
                
                themeLightButton.addEventListener('click', (e) => {
                    e.preventDefault();
                    state.settings.darkMode = false;
                    saveSettings();
                    applySettings();
                });
                
                clearDataButton.addEventListener('click', (e) => {
                    e.preventDefault();
                    clearBrowserData();
                });
                
                aboutBrowserButton.addEventListener('click', (e) => {
                    e.preventDefault();
                    alert('DOROSfamd+ Browser v1.0\nБыстрый и безопасный браузер с блокировщиком рекламы.');
                });
            }

            // Запуск браузера
            initBrowser();
        });
    </script>
</body>
</html>
